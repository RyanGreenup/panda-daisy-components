import { layout } from "@ryangreenup/panda-daisy-components-styled-system/recipes";

// TODO we should storybook by default and hava 100dvh override
export const classesFromRecipe = layout({
  storybook: true,
  btmDash: "mobileOnly",
});
